{
  "name": "gse-cf-openapi",
  "description": "A Cloudflare Worker using Google Programmable Search API and analyzing the results via a OpenAI GPT",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/itty-router-openapi": "^1.0.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.0.0"
  }
}